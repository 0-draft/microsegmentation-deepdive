services:
  localstack:
    image: localstack/localstack:3.8
    container_name: ms08-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=ec2,iam,sts
      - DEBUG=0
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
